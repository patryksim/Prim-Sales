const ios = require('@react-native-community/cli-platform-ios');
const android = require('@react-native-community/cli-platform-android');

module.exports = {
    project: {
        ios: {},
        android: {},
    },
    assets: ['./src/assets/icon/'], // stays the same
    // commands: require('./path-to-commands.js'),
};
